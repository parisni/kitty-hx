#!/usr/bin/env bash

INPUT="$(</tmp/sel.txt)"

if [ -n "$1" ]; then
  scooter --search-text "$INPUT" --print-results -I "$1"
else
  scooter --search-text "$INPUT" --print-results --hidden 
fi

rm -f /tmp/sel.txt

